{
  "name": "portfolio-server",
  "version": "1.0.0",
  "description": "API + BBDD SQL para el portfolio (React + Vite + Bootstrap). Usa node:sqlite, sin dependencias nativas que compilar.",
  "main": "index.js",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "seed": "node seed.js"
  },
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.7.0",
    "helmet": "^8.3.0",
    "nodemailer": "^10.0.9"
  }
}
